function Content() {
  return (
    <div className="sidebar__content">
      <div className="sidebar__block">
        <h3 className="sidebar__block-title">Sidebar Content Title</h3>
        <div className="sidebar__block-content">
          <p className="sidebar__block-text">
            Here's a lot of pre-plan data. Here's a lot of pre-plan data. Here's
            a lot of pre-plan data. Here's a lot of pre-plan data. Here's a lot
            of pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data.Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data.Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data.Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data.Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data.Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data.Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data.Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data.Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data. Here's a lot of pre-plan data. Here's a lot of
            pre-plan data.
          </p>
        </div>
      </div>
    </div>
  );
}

export default Content;