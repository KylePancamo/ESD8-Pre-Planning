################# Instructions #################
# Simple config to deploy a Static SPA React App. 
# The most important field for deploying your React app is root where your build files will be hosted.
# For example, in this case, I copied my build file in the  /home/ubuntu/app-deploy/build directory.
# Since a React project is built of static files that only need a browser, you don’t have to configure any further server-side language. 
################# Instructions  #################

server {
	root /home/azureuser/ESD8-Pre-Planning/app-ui/build;
  server_name esd8.eastus2.cloudapp.azure.com;

  index index.html;

	location / {
    try_files $uri $uri/ /index.html;
  }

  # location /api/ {
  #   proxy_http_version 1.1;
  #   proxy_cache_bypass $http_upgrade;

  #   proxy_set_header Upgrade $http_upgrade;
  #   proxy_set_header Connection 'upgrade';
  #   proxy_set_header Host $host;
  #   proxy_set_header X-Real-IP $remote_addr;
  #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;                
  #   proxy_set_header X-Forwarded-Proto $scheme;

  #   proxy_pass http://20.242.84.130:5000;
  # }

    listen [::]:443 ssl ipv6only=on; # managed by Certbot
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/esd8.eastus2.cloudapp.azure.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/esd8.eastus2.cloudapp.azure.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}


server {
    if ($host = esd8.eastus2.cloudapp.azure.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


  listen 80 default_server;
	listen [::]:80 default_server;
  server_name esd8.eastus2.cloudapp.azure.com;
    return 404; # managed by Certbot


}